Chunking is the process of splitting long documents into smaller units before embedding. A naive approach uses fixed character windows with overlap. A smarter approach respects sentence and paragraph boundaries so that each chunk is semantically coherent. The optimal chunk size depends on the embedding model's context window and the downstream retrieval task. Smaller chunks give finer-grained retrieval; larger chunks preserve more context per result.
