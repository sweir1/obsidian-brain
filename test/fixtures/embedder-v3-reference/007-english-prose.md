Quantization reduces the memory footprint of a neural network by representing weights with fewer bits than the original float32 training precision. Q8 quantization uses 8-bit integers, reducing model size by roughly 4× with minimal accuracy loss on most benchmarks. Q4 cuts size further but shows more degradation on nuanced tasks. For sentence embedding models, Q8 is widely considered the sweet spot for offline consumer hardware.
