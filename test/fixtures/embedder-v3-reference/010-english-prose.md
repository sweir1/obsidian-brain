Incremental indexing is the process of updating only the notes that have changed since the last index run, rather than rebuilding everything from scratch. It requires tracking modification times (mtime) or content hashes for each indexed file, comparing them against the stored baseline, and reprocessing only the diff. In a large vault with thousands of notes, incremental indexing reduces startup latency by orders of magnitude compared to a full rebuild.
