Vector databases store high-dimensional floating-point arrays and answer approximate nearest-neighbour queries efficiently. Common indexing algorithms include HNSW (hierarchical navigable small worlds) and IVF (inverted file index). SQLite-vec brings this capability to SQLite as a loadable extension, enabling vector search without a separate service process. The trade-off against dedicated vector databases is throughput under heavy concurrent write load.
