The Model Context Protocol is an open standard for connecting language models to external tools and data sources. It defines a JSON-RPC transport with a capability negotiation handshake, letting servers declare what tools they expose and what resources they manage. Claude Desktop and Claude Code are MCP clients; obsidian-brain is an MCP server. The protocol is transport-agnostic: stdio, HTTP, and WebSocket are all valid transports.
