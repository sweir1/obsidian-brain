Hybrid search combines dense vector retrieval with sparse keyword retrieval, then re-ranks the merged result set. The BM25 sparse signal catches rare proper nouns and exact identifiers that dense models underrepresent. The dense signal catches paraphrases and semantic variants that keyword matching misses. Fusion strategies include Reciprocal Rank Fusion (RRF), linear interpolation of scores, and learned late-fusion models.
