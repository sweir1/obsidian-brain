Retrieval-augmented generation combines a retrieval step with a generative language model. Rather than relying solely on parametric memory baked into the model weights, RAG fetches relevant passages at inference time and conditions the generator on them. This approach allows the system to stay current with new information without retraining, and allows citations to be grounded in actual source documents, reducing hallucination.
