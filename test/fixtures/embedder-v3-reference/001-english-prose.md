The knowledge graph is a structured representation of information where entities are nodes and relationships are edges. Unlike flat document stores, a knowledge graph captures the semantic connections between concepts, enabling traversal queries that surface non-obvious links. In an Obsidian vault, every note is a potential node, and every wikilink is a directed edge. This structure allows the system to answer questions like "what notes are within two hops of this concept?" without full-text search.
